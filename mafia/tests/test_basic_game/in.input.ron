[
    // Test amending actions.
    Plan("Eve", Order("Eve", Kill("Bob"))),

    // Test faction actions and kills.
    Plan("Eve", Order("Malory", Kill("Alice"))),

    // Resolve Night 0.
    AdvancePhase,
]
