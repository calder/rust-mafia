// Test:
// * Faction actions.
// * Mafia winning by eliminating players.
// * Killing players.
// * Investigating good and evil players.
[
    // Test amending actions.
    Plan("Eve", Order("Eve", Kill("Alice"))),

    // Test faction actions and kills.
    Plan("Eve", Order("Malory", Kill("Charlie"))),

    // Test good investigation.
    Plan("Alice", Investigate("Bob")),

    // Test protection.
    Plan("Bob", Protect("Charlie")),

    // Resolve Night 0.
    AdvancePhase,

    // Resolve Day 1.
    AdvancePhase,

    // Test evil investigation.
    Plan("Alice", Investigate("Eve")),

    // Test mafia win.
    Plan("Eve", Order("Eve", Kill("Charlie"))),

    // Resolve Night 1.
    AdvancePhase,
]
